/**
 * @ngdoc controller
 */
define(['interface'], () => {
  return [
    '$scope',
    conversation
  ]

  /**
   * @name conversation
   * @returns {*} undefined
   */
  function conversation() {
  }
})
